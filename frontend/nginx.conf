server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # React Router: todas las rutas no encontradas van a index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}